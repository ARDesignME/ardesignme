{#
/**
 * @file
 * Displays Social Network link buttons
 *
 * Available variables:
 * image: prints the image url
 * attributes: prints selected attributes from block config
 *
 */
#}

<img src="{{ image }}" {{ attributes }}>
